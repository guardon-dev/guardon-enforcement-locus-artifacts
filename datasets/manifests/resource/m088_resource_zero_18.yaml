apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-app-18
spec:
  selector:
    matchLabels:
      app: resource-app-18
  template:
    metadata:
      labels:
        app: resource-app-18
    spec:
      containers:
      - name: app
        image: nginx
        resources:
          requests:
            cpu: "0"
            memory: "0"
          limits:
            cpu: "0"
            memory: "0"
